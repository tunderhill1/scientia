import { styled } from '../stitches.config'

export const GameElement = styled('div', {
  height: '2.75rem',
  width: '100%',
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  justifyContent: 'center',
  userSelect: 'none',
})
